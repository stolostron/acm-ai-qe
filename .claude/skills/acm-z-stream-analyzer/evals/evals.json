{
  "skill_name": "acm-z-stream-analyzer",
  "version": "4.0.0",
  "evals": [
    {
      "id": 1,
      "prompt": "Analyze any Jenkins pipeline URL with test failures.",
      "expected_output": "Pipeline produces all expected artifacts regardless of input job type (clc, alc, virt, grc).",
      "assertions": [
        "Stage 1 produces core-data.json with test_report, cluster_landscape, and feature_grounding sections",
        "Stage 1.5 produces cluster-diagnosis.json with environment_health_score, subsystem_health, and classification_guidance",
        "Stage 2 produces analysis-results.json with per_test_analysis array, summary.by_classification, and investigation_phases_completed",
        "Stage 3 produces Detailed-Analysis.md and analysis-report.html",
        "Pipeline runs stages in order with visible progress updates"
      ],
      "category": "process"
    },
    {
      "id": 2,
      "prompt": "Classify test failures from a pipeline run with mixed failure types.",
      "expected_output": "Every test gets a classification with multi-evidence support. No test left unclassified.",
      "assertions": [
        "Every failed test has a classification from the 7 valid types: PRODUCT_BUG, AUTOMATION_BUG, INFRASTRUCTURE, NO_BUG, MIXED, FLAKY, UNKNOWN",
        "Every classification has minimum 2 evidence sources",
        "Every classification includes ruled_out_alternatives explaining why other types don't fit",
        "Every classification includes root_cause_layer (1-12) and root_cause_layer_name"
      ],
      "category": "structure"
    },
    {
      "id": 3,
      "prompt": "Analyze a pipeline run where the cluster has a single infrastructure issue but tests fail for multiple reasons.",
      "expected_output": "A single cluster-wide issue does NOT auto-classify all failures. Each test gets individual verification. Mixed failure sets are classified independently.",
      "assertions": [
        "Each test gets individual counterfactual verification even when cluster health is degraded",
        "Tests with non-infrastructure root causes (stale selectors, wrong assertions) are NOT classified as INFRASTRUCTURE",
        "Per-test causal link verification connects the specific cluster issue to each test's specific error",
        "Tests failing for different reasons in the same pipeline run get different classifications"
      ],
      "category": "edge-case"
    },
    {
      "id": 4,
      "prompt": "Analyze pipeline runs with varied failure modes: selector not found, timeout, assertion failure, page crash, hook failure, API error.",
      "expected_output": "Each failure type is routed through the correct investigation path. No pattern-matching to previously seen failures.",
      "assertions": [
        "Selector not found: triggers MCP verification via acm-source MCP before classifying",
        "Timeout failures: checks subsystem health and backend state before attributing to infrastructure",
        "After-all hook failures: correctly identified as cascade from prior failure, classified as NO_BUG",
        "Each failure type follows its investigation path generically, not by matching known error strings"
      ],
      "category": "edge-case"
    },
    {
      "id": 5,
      "prompt": "Analyze a Jenkins run that was ABORTED (or has zero failed tests), and run it with optional MCP servers (neo4j-rhacm, acm-search) unavailable.",
      "expected_output": "The pipeline degrades gracefully: it still produces a valid report for the empty/ABORTED build, and never aborts when an optional MCP is missing.",
      "assertions": [
        "When build_result is ABORTED, Stage 2 writes a minimal analysis-results.json with per_test_analysis [], summary.by_classification {}, investigation_phases_completed [], and analysis_metadata.build_result 'ABORTED'",
        "Stage 3 renders a 'Build Aborted' section from the minimal analysis-results.json and report.py exits successfully",
        "When there are zero failed tests or build_result is NOT_BUILT, Stage 2 output is skipped (writing an empty per_test_analysis with a non-ABORTED build_result would make report.py raise); with no analysis-results.json, Stage 3 renders from core-data.json",
        "When neo4j-rhacm is unavailable, Phase C cascade detection falls back to dependency-chains.yaml instead of aborting",
        "A missing optional MCP (jira, polarion, acm-search, acm-kubectl) degrades only that step and is recorded in the pipeline summary, never aborting the pipeline"
      ],
      "category": "degradation"
    }
  ]
}
