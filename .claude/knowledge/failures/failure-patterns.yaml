# Known Failure Patterns -- Z-Stream Analysis
# Signatures that short-circuit investigation. When a test error matches one
# of these patterns, the AI agent can classify with high confidence without
# full investigation.
#
# Populated by: manual + learned/new-patterns.yaml promotion
# Refresh frequency: after each analysis run (agent can append to learned/)

patterns:

  # --- Selector/UI patterns ---
  - id: carbon-selector
    category: selector
    signature: "tf--list-box|tf--dropdown|tf--combo-box|tf--text-input"
    classification: AUTOMATION_BUG
    confidence: 0.95
    explanation: "Carbon Design System selector. Console migrated to PatternFly in 2023."
    fix: "Replace with PatternFly equivalent (pf-v6-c-menu, pf-v6-c-select, etc.)"

  - id: pf5-selector-deprecated
    category: selector
    signature: "pf-c-[a-z]|pf-v5-c-[a-z]"
    classification: AUTOMATION_BUG
    confidence: 0.85
    explanation: "PatternFly 5 selector. Console migrated to PF6."
    fix: "Replace pf-c- or pf-v5-c- prefix with pf-v6-c-."

  - id: pf6-portal-visibility
    category: selector
    signature: "pf-v6-c-menu__item.*visibility.*hidden"
    classification: AUTOMATION_BUG
    confidence: 0.90
    explanation: "PF6 renders menu items in a portal outside .within() scope."
    fix: "Use { withinSubject: null } or cy.get() outside .within() block."

  - id: data-ouia-deprecated
    category: selector
    signature: "data-ouia-component-id.*not found|data-ouia-component-type.*not found"
    classification: AUTOMATION_BUG
    confidence: 0.80
    explanation: "OUIA selectors may have changed or been removed in PF6 migration."
    fix: "Check current component for data-ouia attributes. May need data-test-id instead."

  # --- Infrastructure patterns ---
  - id: vm-scheduling-no-kvm
    category: infrastructure
    signature: "FailedScheduling.*devices\\.kubevirt\\.io/kvm"
    classification: INFRASTRUCTURE
    confidence: 0.95
    explanation: "No KVM-capable worker nodes available for VM scheduling."
    fix: "Add KVM-capable nodes or use a cluster with nested virtualization."

  - id: certificate-expired
    category: infrastructure
    signature: "x509.*certificate.*expired|tls.*handshake.*failure|certificate.*not valid"
    classification: INFRASTRUCTURE
    confidence: 0.85
    explanation: "TLS certificate expired or invalid."
    fix: "Check cert expiry. If service-ca managed, check service-ca-operator health."

  - id: webhook-connection-refused
    category: infrastructure
    signature: "failed calling webhook.*connection refused|webhook.*service.*unavailable"
    classification: INFRASTRUCTURE
    confidence: 0.90
    explanation: "Webhook service pod is down. Operations that require validation will fail."
    fix: "Check webhook service pod health. Restart if needed."

  - id: etcd-leader-election
    category: infrastructure
    signature: "etcdserver.*leader changed|raft.*election"
    classification: INFRASTRUCTURE
    confidence: 0.70
    explanation: "etcd leader election in progress. Temporary API instability."
    fix: "Usually self-resolving. Check etcd pod health and node stability."

  - id: node-not-ready
    category: infrastructure
    signature: "NodeNotReady|node.*not.*ready|SchedulingDisabled"
    classification: INFRASTRUCTURE
    confidence: 0.85
    explanation: "One or more nodes are NotReady. Pod scheduling fails."
    fix: "Check node conditions and events."

  # --- Product bug patterns ---
  - id: nil-pointer-panic
    category: product
    signature: "runtime error.*nil pointer dereference|panic.*nil pointer"
    classification: PRODUCT_BUG
    confidence: 0.90
    explanation: "Go nil pointer dereference in controller code."
    fix: "File JIRA bug with stack trace."

  - id: reconciliation-hot-loop
    category: product
    signature: "Reconciling.*Reconciling.*Reconciling|conflict.*retry"
    classification: PRODUCT_BUG
    confidence: 0.70
    explanation: "Controller in reconciliation hot loop. May indicate race condition."
    fix: "Check controller logs for resource conflict details."

  # --- Automation patterns ---
  - id: cy-visit-timeout
    category: automation
    signature: "cy\\.visit.*timed out|Timed out retrying.*cy\\.visit"
    classification: AUTOMATION_BUG
    confidence: 0.65
    explanation: "Cypress visit timeout. Could be slow page load or wrong URL."
    fix: "Check if URL is correct and page actually loads."
    note: "Low confidence -- needs backend health check to distinguish from INFRASTRUCTURE."

  - id: hook-cascade
    category: automation
    signature: "because this error occurred during.*after all.*hook|after each.*hook"
    classification: NO_BUG
    confidence: 0.90
    explanation: "After-all/after-each hook failure cascading from prior test failure."
    fix: "Classify the original test failure, not the hook cascade."

  - id: blank-page-no-js
    category: infrastructure_prereq
    signature: "no-js|blank.*page|<body>\\s*</body>|empty.*body"
    classification: INFRASTRUCTURE
    confidence: 0.85
    explanation: "Page loads but has no content. Usually missing prerequisite (AAP, IDP, CNV)."
    fix: "Check if the feature prerequisite is installed on the cluster."
    requires_verification: true
    note: "Must verify which prerequisite is missing before finalizing."

  # --- External service patterns ---
  - id: minio-objectstore-down
    category: external_service
    signature: "minio.*connection.*(?:refused|timeout)|objectstore.*(?:fail|error|unavailable)"
    classification: INFRASTRUCTURE
    confidence: 0.85
    explanation: "External Minio/S3 object storage server is unreachable. Object Storage subscription tests fail because the channel endpoint is down."
    fix: "Check OBJECTSTORE_PRIVATE_URL from Jenkins params. Verify Minio server health on the remote cluster."

  - id: gogs-git-push-failure
    category: external_service
    signature: "failed to push to testrepo|gogs.*connection.*(?:refused|error)"
    classification: INFRASTRUCTURE
    confidence: 0.85
    explanation: "External Gogs Git server is down or unreachable. Git channel subscription tests fail at setup."
    fix: "Check Gogs deployment on the CI runner. Verify the Git server endpoint is reachable."

  - id: mtls-setup-failure
    category: external_service
    signature: "MTLS Test Environment setup failure|SSL certificate problem.*self signed"
    classification: INFRASTRUCTURE
    confidence: 0.85
    explanation: "MTLS test environment failed to initialize. Gogs with custom TLS certs did not start properly."
    fix: "Check SSL cert chain for the Gogs mTLS server. Verify cert generation in CI setup."

  - id: tower-unreachable
    category: external_service
    signature: "tower.*(?:unreachable|connection.*refused|timeout)|ansible.*tower.*(?:fail|error)"
    classification: INFRASTRUCTURE
    confidence: 0.80
    explanation: "Ansible Tower/AAP endpoint is unreachable. AnsibleJob tests fail."
    fix: "Check TOWER_HOST from Jenkins params. Verify Tower is running and API is accessible."
    note: "If AAP >= 2.5 and test uses workflow jobs, check version-constraints.yaml -- may be PRODUCT_BUG instead."

# --- Known JIRA bugs ---
# Cached JIRA references for known failures. The AI agent checks these
# BEFORE calling the JIRA MCP tool, providing instant correlation.
# Entries can become stale -- always verify the JIRA is still open/relevant.
#
# Refresh frequency: weekly or after each z-stream analysis run

known_jira_bugs:

  - id: acm-32244-subscription-timestamp
    jira: ACM-32244
    summary: "Timestamp comparison issue causes subscription controller to skip reconciliation"
    affected_components: [subscription-controller, multicloud-operators-subscription]
    feature_area: Application
    symptoms:
      - "subscription is not ready within time limit"
      - "subscription.*not.*reconcil"
      - "appsub.*timeout"
    classification: PRODUCT_BUG
    confidence: 0.80
    versions_affected: ["2.13", "2.14"]
    fix_version: "2.14.1"
    status: open
    last_verified: "2026-03-31"
    note: >
      Subscription controller skips reconciliation when lastTransitionTime comparison
      fails due to timezone/precision mismatch. Only applies when the subscription-controller
      pod is Running/Ready but subscriptions are not reconciling. If the pod is unhealthy,
      classify as INFRASTRUCTURE instead. See disambiguation in
      knowledge/architecture/application-lifecycle/failure-signatures.md.

  # --- Promoted from learned/feature-gaps.yaml (batch) ---
  - id: perspective-switcher-race-condition
    category: configuration
    signature: "(?i)Expected to find element.*cluster-dropdown-toggle"
    classification: AUTOMATION_BUG
    confidence: 0.9
    explanation: "Synchronous jQuery check races with async perspective switcher rendering, falls through to removed cluster-dropdown-togg"
  - id: cluster-dropdown-toggle-dead
    category: configuration
    signature: "(?i)Expected to find element.*cluster-dropdown-toggle"
    classification: AUTOMATION_BUG
    confidence: 0.95
    explanation: "OCP 4.20 replaced cluster-dropdown-toggle with perspective-switcher-toggle"
  - id: pre-upgrade-text-renamed
    category: configuration
    signature: "(?i)Expected to find.*Pre-upgrade Ansible templates"
    classification: AUTOMATION_BUG
    confidence: 0.95
    explanation: "Product UI text changed from Pre-upgrade to Pre-update, test expects old text"
  - id: ouia-component-id-not-found
    category: configuration
    signature: "(?i)Expected to find element.*data-ouia-component-id"
    classification: AUTOMATION_BUG
    confidence: 0.85
    explanation: "OUIA component ID value is derived from test data (user name), not a static product selector"
  - id: tree-view-pf6-not-rendered
    category: connectivity
    signature: "(?i)Expected to find element.*pf-v6-c-tree-view"
    classification: INFRASTRUCTURE
    confidence: 0.7
    explanation: "TreeView component exists in kubevirt-plugin but may not render if plugin not loaded or page not ready"
  - id: yaml-parse-error-credentials
    category: data_flow
    signature: "(?i)end of the stream or a document separator is expected"
    classification: AUTOMATION_BUG
    confidence: 0.75
    explanation: "Test fixture or form produces invalid YAML content in credential creation"
  - id: view-history-text-missing
    category: configuration
    signature: "(?i)Expected to find content.*View history"
    classification: AUTOMATION_BUG
    confidence: 0.7
    explanation: "Policy history link text may have been renamed in PF6 migration"
  - id: gitops-addon-undefined-namespace
    category: configuration
    signature: "(?i)oc apply.*gitops-addon\.yaml.*-n undefined"
    classification: AUTOMATION_BUG
    confidence: 0.85
    explanation: "Test setup resolves managed cluster namespace to JS 'undefined' literal, compounded by ResourceQuota blocking ALC contro"
  - id: resourcequota-blocks-alc-controllers
    category: component_health
    signature: "(?i)Error from server.*NotFound.*error when creating"
    classification: INFRASTRUCTURE
    confidence: 0.95
    explanation: "ResourceQuota restrict-ocm limits pods=5 but 20 pods exist, blocking hub-subscription, channel, subscription-report cont"
  - id: networkpolicy-blocking-search-postgres
    category: connectivity
    signature: "(?i)search.*empty.*results"
    classification: INFRASTRUCTURE
    confidence: 0.9
    explanation: "NetworkPolicy with empty ingress rules blocks search-postgres, plus search-v2-operator at 0/1 from ResourceQuota"
  - id: rbac-gitops-addon-hook-failure
    category: configuration
    signature: "(?i)oc apply.*gitops-addon\.yaml.*-n undefined"
    classification: AUTOMATION_BUG
    confidence: 0.85
    explanation: "Same root cause as ALC: managed cluster namespace variable is undefined"
  - id: deployment-not-ready-timeout
    category: component_health
    signature: "(?i)Deployment is not ready within time limit"
    classification: INFRASTRUCTURE
    confidence: 0.8
    explanation: "Subscription Reconciliation Timeout — classification depends on controller health"
  - id: before-all-hook-timeout
    category: prerequisite
    signature: "(?i)Timed out retrying.*before all.*hook"
    classification: INFRASTRUCTURE
    confidence: 0.75
    explanation: "before-all hook timeouts indicate environment setup failure"
  - id: argocd-agent-pod-not-ready
    category: component_health
    signature: "(?i)ArgoCD Agent.*pod is not ready within time limit"
    classification: INFRASTRUCTURE
    confidence: 0.8
    explanation: "ArgoCD agent pod not reaching Ready state blocks all ArgoCD-based tests"
  - id: pf6-menu-portal-visibility
    category: configuration
    signature: "(?i)cy\.click\(\) failed because this element is not visible.*pf-v6-c-menu__item-text"
    classification: AUTOMATION_BUG
    confidence: 0.9
    explanation: "PF6 menu portals render outside .within() scope"
  - id: cy-exec-cleanup-failure
    category: configuration
    signature: "(?i)cy\.exec.*failed because the command exited with a non-zero code"
    classification: NO_BUG
    confidence: 0.85
    explanation: "After-all cleanup fails because the resource was never created"
  - id: pf6-button-visibility
    category: configuration
    signature: "(?i)cy\.click\(\) failed because this element.*pf-v6-c-button__text.*has CSS"
    classification: AUTOMATION_BUG
    confidence: 0.85
    explanation: "PF6 button text elements may have visibility:hidden in portal context"
  - id: managed-cluster-context-switch-failed
    category: connectivity
    signature: "(?i)Failed to switch context to managed cluster"
    classification: INFRASTRUCTURE
    confidence: 0.85
    explanation: "Test setup requires oc context switch to managed cluster, failure indicates cluster-proxy connectivity issue or managed "
  - id: kubevirt-crd-check-timeout
    category: prerequisite
    signature: "(?i)cy\.exec.*oc get crd virtualmachines\.kubevirt\.io.*timed out"
    classification: INFRASTRUCTURE
    confidence: 0.9
    explanation: "Before-all hook checks for KubeVirt CRD via oc exec, timeout indicates CNV not installed or API server unreachable"
  - id: cy-click-multiple-elements
    category: configuration
    signature: "(?i)cy\.click\(\).*can only be called on a single element.*contained \d+ elements"
    classification: AUTOMATION_BUG
    confidence: 0.9
    explanation: "Test selector matches multiple DOM elements but cy.click() expects single element"
  - id: pf-table-row-covered-by-action-list
    category: configuration
    signature: "(?i)pf-v6-c-table__tr.*not visible.*position.*fixed.*pf-v6-c-action-list"
    classification: PRODUCT_BUG
    confidence: 0.8
    explanation: "Table row in Role Assignment wizard covered by ActionList overlay from CreateIdentityForm.tsx"
  - id: pre-authorized-user-text-changed
    category: configuration
    signature: "(?i)Expected to find content.*add pre-authorized user"
    classification: AUTOMATION_BUG
    confidence: 0.95
    explanation: "Commit 639e721d0 (2026-03-25, ACM-31178) changed button title in IdentitiesList.tsx"
  - id: vm-cleanup-timeout
    category: configuration
    signature: "(?i)cy\.exec.*oc delete virtualmachine.*timed out"
    classification: NO_BUG
    confidence: 0.85
    explanation: "After-all cleanup fails because VM was never created (cascading from before-all failure)"
  - id: role-assignment-button-not-enabled
    category: data_flow
    signature: "(?i)Create role assignment button did not become enabled"
    classification: PRODUCT_BUG
    confidence: 0.7
    explanation: "Role assignment wizard button remains disabled, product validation issue or data loading failure preventing form submiss"
  - id: vm-actions-dropdown-not-found
    category: data_flow
    signature: "(?i)Expected to find element.*actions-dropdown.*button.*never found"
    classification: INFRASTRUCTURE
    confidence: 0.75
    explanation: "data-test="actions-dropdown" exists in kubevirt-plugin source but not rendered at runtime, VM may not exist on the page "
  - id: app-not-ready-timeout
    category: data_flow
    signature: "(?i)Application is not ready within time limit"
    classification: INFRASTRUCTURE
    confidence: 0.8
    explanation: "Subscription Reconciliation Timeout — controllers healthy per cluster diagnostic, failure is in downstream deployment ch"
  - id: argocd-appset-wizard-timeout
    category: data_flow
    signature: "(?i)Timed out retrying"
    classification: INFRASTRUCTURE
    confidence: 0.7
    explanation: "Generic timeout from cypress-wait-until during Argo appset wizard, matches ArgoCD Sync Stuck pattern"
  - id: corrupted-bash-cy-exec
    category: configuration
    signature: "(?i)/usr/bin/bash:.*which.*syntax error"
    classification: INFRASTRUCTURE
    confidence: 0.9
    explanation: "CI runner bash has corrupted which function definition, cy.exec() commands inherit this environment"
  - id: subscription-ns-not-found
    category: data_flow
    signature: "(?i)cy\.exec.*oc.*get subscription.*failed.*non-zero"
    classification: INFRASTRUCTURE
    confidence: 0.8
    explanation: "Subscription namespace never created because upstream deployment failed (channel connectivity or placement issue)"
